#include <iostream>
#include <string>
using namespace std;
int main() {
    int goles;
    int i;
    char inicial;
    bool leyenda;
    string nombre;
    float promedio;
    int x;
    bool t18 = goles > 11;
    bool t2 = goles > 10;
    bool t22 = i < 5;
    bool t28 = goles > 10;
    bool t40 = goles > 11;
    bool t44 = i < 5;
    bool t48 = x < 3;
    bool t50 = x < 3;
    bool t52 = x < 3;
    bool t54 = x < 3;
    bool t56 = x < 3;
    bool t58 = x < 3;
    bool t60 = x < 3;
    bool t62 = x < 3;
    goles = 10;
    promedio = 7.5;
    inicial = 'M';
    leyenda = true;
    nombre = "Messi";
    goles = goles + 5;
    // INICIO WHILE;
    // INICIO WHILE;
    while (!(goles > 10)) {
        promedio = promedio + 0.5;
        goles = goles - 1;
        promedio = promedio + 0.5;
        goles = goles - 1;
        promedio = promedio + 0.5;
        goles = goles - 1;
        promedio = promedio + 0.5;
        goles = goles - 1;
        promedio = promedio + 0.5;
        goles = goles - 1;
    } // FIN while
    // FIN WHILE;
    // INICIO IF;
    // INICIO WHILE;
    while (!(goles > 11)) {
        promedio = promedio + 1.0;
    } // FIN while
    // ELSE;
    promedio = promedio - 0.5;
    // FIN IF;
    i = 0;
    //INICIO DO-WHILE;
    i = i + 1;
    // INICIO WHILE;
    while ((t22)) {
        //FIN DO-WHILE;
        // SWITCH_START goles;
        // CASE 10;
        promedio = promedio + 2.0;
    } // FIN while
    // BREAK;
    // CASE 15;
    promedio = promedio + 5.0;
    // BREAK;
    // DEFAULT;
    promedio = 0;
    // SWITCH_END;
    goles = goles + 5;
    // INICIO WHILE;
    // INICIO WHILE;
    while (!(goles > 10)) {
        goles = goles - 1;
        goles = goles - 1;
        goles = goles - 1;
        goles = goles - 1;
        goles = goles - 1;
    } // FIN while
    // FIN WHILE;
    // INICIO IF;
    // INICIO IF;
    if (!(!(t40))) {
        // ELSE;
        promedio = promedio - 0.5;
        // FIN IF;
        i = 0;
        //INICIO DO-WHILE;
        i = i + 1;
        // INICIO IF;
        if ((t44)) {
            //FIN DO-WHILE;
            // SWITCH_START goles;
            // CASE 10;
            promedio = promedio + 2.0;
        } // FIN if
        // BREAK;
        // CASE 15;
        promedio = promedio + 5.0;
    } // FIN if
    // BREAK;
    // DEFAULT;
    promedio = 0;
    // SWITCH_END;
    x = 0;
    // INICIO FOR;
    // INICIO IF;
    if (!(x < 3)) {
        x = x + 1;
        // INICIO IF;
        if (!(x < 3)) {
        } // FIN if
        x = x + 1;
        // INICIO IF;
        if (!(x < 3)) {
        } // FIN if
        x = x + 1;
        // INICIO WHILE;
        while (!(!(t54))) {
            // FIN FOR;
            x = 0;
            // INICIO FOR;
            // INICIO IF;
            if (!(x < 3)) {
                x = x + 1;
                // INICIO IF;
                if (!(x < 3)) {
                } // FIN if
                x = x + 1;
                // INICIO IF;
                if (!(x < 3)) {
                } // FIN if
                x = x + 1;
                // INICIO IF;
                if (!(!(t62))) {
                    // FIN FOR;
                } // FIN if
            } // FIN if
        } // FIN while
    } // FIN if
    return 0;
}