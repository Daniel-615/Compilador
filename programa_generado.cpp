#include <iostream>
#include <string>
using namespace std;
int main() {
    int goles;
    int i;
    char inicial;
    bool leyenda;
    string nombre;
    float promedio;
    int x;
    goles = 10;
    promedio = 7.5;
    inicial = 'M';
    leyenda = true;
    nombre = "Messi";
    goles = goles + 5;
    // INICIO WHILE;
    // INICIO WHILE;
    while (!(0)) {
        promedio = promedio + 0.5;
        goles = goles - 1;
        promedio = promedio + 0.5;
        goles = goles - 1;
        promedio = promedio + 0.5;
        goles = goles - 1;
        promedio = promedio + 0.5;
        goles = goles - 1;
        promedio = promedio + 0.5;
        goles = goles - 1;
    } // FIN while
    // FIN WHILE;
    // INICIO IF;
    // INICIO WHILE;
    while (!(cond_if_0)) {
        promedio = promedio + 1.0;
    } // FIN while
    // ELSE;
    promedio = promedio - 0.5;
    // FIN IF;
    i = 0;
    //INICIO DO-WHILE;
    i = i + 1;
    i = i + 1;
    i = i + 1;
    i = i + 1;
    i = i + 1;
    // INICIO WHILE;
    while (1) {
        //FIN DO-WHILE;
        // SWITCH_START goles;
        // CASE 10;
        promedio = promedio + 2.0;
    } // FIN while
    // BREAK;
    // CASE 15;
    promedio = promedio + 5.0;
    // BREAK;
    // DEFAULT;
    promedio = 0;
    // SWITCH_END;
    goles = goles + 5;
    // INICIO WHILE;
    // INICIO WHILE;
    while (!(2)) {
        goles = goles - 1;
        goles = goles - 1;
        goles = goles - 1;
        goles = goles - 1;
        goles = goles - 1;
    } // FIN while
    // FIN WHILE;
    // INICIO IF;
    // INICIO WHILE;
    while (!(cond_if_1)) {
    } // FIN while
    // ELSE;
    promedio = promedio - 0.5;
    // FIN IF;
    i = 0;
    //INICIO DO-WHILE;
    i = i + 1;
    i = i + 1;
    i = i + 1;
    i = i + 1;
    i = i + 1;
    // INICIO WHILE;
    while (3) {
        //FIN DO-WHILE;
        // SWITCH_START goles;
        // CASE 10;
        promedio = promedio + 2.0;
    } // FIN while
    // BREAK;
    // CASE 15;
    promedio = promedio + 5.0;
    // BREAK;
    // DEFAULT;
    promedio = 0;
    // SWITCH_END;
    x = 0;
    // INICIO FOR;
    // INICIO WHILE;
    while (!(4)) {
        x = x + 1;
        x = x + 1;
        x = x + 1;
    } // FIN while
    // FIN FOR;
    x = 0;
    // INICIO FOR;
    // INICIO WHILE;
    while (!(5)) {
        x = x + 1;
        x = x + 1;
        x = x + 1;
    } // FIN while
    // FIN FOR;
    return 0;
}